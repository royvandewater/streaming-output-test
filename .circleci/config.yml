version: 2.1

definitions:
  do-build: &do-build
    docker:
      - image: bash:5.0.7
    steps:
      - checkout
      - run: ./main.sh
      - run: circleci test split


jobs:
  build: *do-build

workflows:
  test:
    jobs:
      - build
